{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "bash -c 'base=\"${VERCEL_GIT_PREVIOUS_SHA:-}\"; git cat-file -e \"$base^{commit}\" 2>/dev/null || base=\"HEAD^\"; git cat-file -e \"$base^{commit}\" 2>/dev/null || exit 1; git diff --quiet \"$base\" HEAD -- . \":(exclude)katalog\" && exit 0 || exit 1'",
  "github": { "autoJobCancelation": false }
}
